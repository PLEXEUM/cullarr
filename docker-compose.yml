services:
  cullarr:
    build: .
    container_name: cullarr
    ports:
      - "7447:7447"
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
    environment:
      - TZ=UTC
      - LOG_LEVEL=INFO
      - MAX_LOG_FILES=5
      - LOG_MAX_SIZE_MB=10
    restart: unless-stopped
    cpu_count: 1
    mem_limit: 512m